{
  "collectionName": "components_shared_buttons",
  "info": {
    "displayName": "Button"
  },
  "options": {},
  "attributes": {
    "Text": {
      "type": "string"
    },
    "Type": {
      "type": "enumeration",
      "enum": [
        "link",
        "filled",
        "outline"
      ]
    },
    "Color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "selected",
        "success",
        "warning"
      ]
    },
    "size": {
      "type": "enumeration",
      "enum": [
        "normal",
        "big",
        "small"
      ]
    },
    "URL": {
      "type": "string"
    }
  },
  "config": {}
}
